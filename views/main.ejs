<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title>صفحه اصلی</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #001f3f; 
      color: white;
      font-family: Arial, sans-serif;
    }
    header {
      display: flex;
      justify-content: flex-end;
      padding: 20px;
    }
    header a img {
      width: 40px;
      height: 40px;
      cursor: pointer;
    }
    main {
      text-align: center;
      margin-top: 50px;
    }
    .courses {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      max-width: 1000px;
      margin: 40px auto;
    }
    .card {
      background: #fff;
      color: #000;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0,0,0,0.2);
      transition: 0.3s;
    }
    .card:hover {
      transform: translateY(-5px);
    }
    .card img {
      width: 100%;
      height: 150px;
      object-fit: cover;
    }
    .card-content {
      padding: 15px;
      text-align: center;
    }
    .card-content h3 {
      margin: 10px 0;
    }
    .card-content p {
      margin: 5px 0;
      font-weight: bold;
    }
    .card-content a {
      display: inline-block;
      margin-top: 10px;
      padding: 8px 15px;
      border: none;
      background: #007bff;
      color: white;
      border-radius: 8px;
      cursor: pointer;
      text-decoration: none;
    }
    .card-content a:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>
  <header>
    <a href="/loginscr">
      <img src="https://cdn-icons-png.flaticon.com/512/149/149071.png" alt="login icon">
    </a>
  </header>
<main>
  <h1>به فروشگاه دوره‌های برنامه‌نویسی خوش اومدی 👨‍💻</h1>
  <p>اینجا می‌تونی بهترین دوره‌ها رو ببینی و بخری.</p>

  <!-- بخش نمایش دوره‌ها -->
  <div class="courses">
    <% if (data && data.length > 0) { %>
      <% data.forEach(course => { %>
        <div class="card">
          <img src="/coursepic/<%= course.background %>" alt="<%= course.title %>">
          <div class="card-content">
            <h3><%= course.title %></h3>
            <p>قیمت: <%= course.price %> تومان</p>
            <a href="/show/<%= course.courseid %>">مشاهده</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p>هیچ دوره‌ای موجود نیست.</p>
    <% } %>
  </div>
</main>

</body>
</html>
