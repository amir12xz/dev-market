<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>دوره‌های من</title>
  <style>
    :root { --purple: #6a0dad; --white: #fff; --card: rgba(255,255,255,0.12); }
    body {
      margin: 0;
      min-height: 100vh;
      background: var(--purple);
      font-family: Vazirmatn, IRANSans, Tahoma, Arial, sans-serif;
      color: var(--white);
      display: grid;
      place-items: center;
      padding: 20px;
    }
    .card {
      width: min(95vw, 600px);
      background: var(--card);
      backdrop-filter: blur(6px);
      border: 1px solid rgba(255,255,255,0.2);
      border-radius: 20px;
      padding: 24px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
    }
    h1 {
      margin: 0 0 20px;
      font-size: 1.5rem;
      font-weight: 800;
      text-align: center;
    }
    ul { list-style: none; padding: 0; margin: 0; }
    li {
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.18);
      border-radius: 14px;
      padding: 14px;
      margin-bottom: 12px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .title { font-weight: 700; font-size: 1rem; }
    .btn {
      background: rgba(255,255,255,0.9);
      color: #4b007b;
      padding: 8px 12px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 600;
      font-size: .9rem;
      transition: .2s;
    }
    .btn:hover { opacity: .85; }
    .empty {
      text-align: center;
      opacity: .8;
      font-size: .95rem;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <main class="card">
    <h1>دوره‌های خریداری شده</h1>

    <% if (courses && courses.length > 0) { %>
      <ul>
        <% courses.forEach(course => { %>
          <li>
            <span class="title"><%= course.title || "بدون نام" %></span>
            <a class="btn" href="/courses/<%= course._id %>">مشاهده</a>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="empty">هنوز هیچ دوره‌ای خریداری نکرده‌اید.</p>
    <% } %>

  </main>
</body>
</html>
